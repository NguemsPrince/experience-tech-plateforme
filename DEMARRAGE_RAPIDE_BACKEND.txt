━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 DÉMARRAGE RAPIDE DU BACKEND - EXPÉRIENCE TECH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME: "La connexion a échoué - Firefox ne peut établir de connexion avec le serveur à l'adresse localhost:5000"

CAUSE: Le serveur backend n'est pas démarré ou MongoDB n'est pas en cours d'exécution.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ SOLUTION RAPIDE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option 1: Script de Démarrage Complet (RECOMMANDÉ)
───────────────────────────────────────────────────────────────

cd /Users/nguemsprince/Desktop/Projet
./demarrer-backend-complet.sh

Ce script démarre automatiquement:
  ✅ MongoDB (si non démarré)
  ✅ Backend (sur le port 5000)
  ✅ Vérifie que tout fonctionne


Option 2: Script de Démarrage Simple
───────────────────────────────────────────────────────────────

cd /Users/nguemsprince/Desktop/Projet
./start-backend.sh

Ce script démarre MongoDB et le backend dans un seul terminal.


Option 3: Démarrage Manuel
───────────────────────────────────────────────────────────────

1. Démarrer MongoDB:
   cd /Users/nguemsprince/Desktop/Projet
   ./mongodb-macos-x86_64-7.0.5/bin/mongod \
       --dbpath ./mongodb-data \
       --port 27017 \
       --logpath ./mongodb.log \
       --fork

2. Vérifier MongoDB:
   lsof -i :27017

3. Démarrer le Backend:
   cd /Users/nguemsprince/Desktop/Projet/backend
   export PATH="/Users/nguemsprince/Desktop/Projet/node-v18.19.0-darwin-x64/bin:$PATH"
   node server.js

4. Vérifier le Backend:
   Ouvrez http://localhost:5000/api/health dans votre navigateur

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 VÉRIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vérifier MongoDB:
  lsof -i :27017
  ps aux | grep mongod

Vérifier le Backend:
  lsof -i :5000
  curl http://localhost:5000/api/health

Vérifier les Logs:
  tail -f /Users/nguemsprince/Desktop/Projet/mongodb.log
  tail -f /Users/nguemsprince/Desktop/Projet/backend.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🛑 ARRÊTER LES SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Arrêter MongoDB:
  pkill -f mongod

Arrêter le Backend:
  lsof -ti:5000 | xargs kill -9

Arrêter Tout:
  pkill -f mongod && lsof -ti:5000 | xargs kill -9

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐛 RÉSOLUTION DES PROBLÈMES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Problème 1: MongoDB ne démarre pas
  - Libérer le port 27017: lsof -ti:27017 | xargs kill -9
  - Vérifier les permissions: ls -la mongodb-data
  - Créer le répertoire: mkdir -p mongodb-data

Problème 2: Le Backend ne démarre pas
  - Vérifier MongoDB: lsof -i :27017
  - Libérer le port 5000: lsof -ti:5000 | xargs kill -9
  - Vérifier les dépendances: cd backend && npm install

Problème 3: Le Backend démarre mais ne répond pas
  - Vérifier les logs: tail -f backend.log
  - Vérifier la configuration: cat backend/.env
  - Tester MongoDB: ./mongodb-macos-x86_64-7.0.5/bin/mongosh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 URLs UTILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend API:     http://localhost:5000
Health Check:    http://localhost:5000/api/health
API Auth:        http://localhost:5000/api/auth
MongoDB:         mongodb://localhost:27017

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📞 SUPPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Si vous rencontrez toujours des problèmes:
  1. Vérifiez les logs (mongodb.log et backend.log)
  2. Vérifiez que tous les ports sont libres
  3. Vérifiez la configuration dans backend/.env
  4. Consultez GUIDE_DEMARRAGE_BACKEND.md pour plus de détails

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

